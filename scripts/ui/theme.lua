--- UI 主题：末世行商配色方案
--- 设计原则：低对比度 · 米白 / 铁灰 / 锈红 · 末世废土感
local M = {}

-- ============================================================
-- 末世废土调色板
-- ============================================================
M.colors = {
    -- 背景层次（暗灰-铁灰系）
    bg_primary   = { 32, 30, 28, 255 },   -- 深铁灰（主背景）
    bg_secondary = { 42, 40, 37, 255 },   -- 暖灰（次背景/顶栏）
    bg_card      = { 52, 48, 44, 240 },   -- 暖深灰（卡片）
    bg_overlay   = { 15, 14, 12, 180 },   -- 遮罩层

    -- 强调色
    accent     = { 198, 156, 82, 255 },   -- 旧铜金（贸易/货币）
    accent_dim = { 198, 156, 82, 100 },
    danger     = { 168, 62, 52, 255 },     -- 锈红（危险/伤害）
    success    = { 108, 148, 96, 255 },    -- 暗绿（收益/安全）
    info       = { 112, 142, 168, 255 },   -- 灰蓝（信息/导航）
    warning    = { 188, 148, 68, 255 },    -- 暗金（警告）

    -- 文字（低对比度米白系）
    text_primary   = { 215, 208, 195, 255 },  -- 米白
    text_secondary = { 158, 152, 140, 255 },  -- 灰褐
    text_dim       = { 108, 102, 92, 255 },   -- 暗灰
    text_accent    = { 198, 156, 82, 255 },   -- 旧铜金

    -- UI 元素
    border       = { 72, 68, 60, 180 },    -- 暗铁灰边框
    divider      = { 62, 58, 52, 150 },
    progress_bg  = { 48, 44, 40, 255 },
    progress_fill = { 108, 148, 96, 255 }, -- 暗绿

    -- 按钮
    btn_primary  = { 92, 110, 128, 255 },  -- 钢蓝灰
    btn_danger   = { 138, 52, 42, 255 },   -- 暗锈红

    -- 地图专用
    map_bg        = { 28, 26, 24, 255 },   -- 地图底色
    map_grid      = { 45, 42, 38, 80 },    -- 网格线
    map_road      = { 128, 122, 108, 200 },-- 主干道（灰白）
    map_path      = { 108, 102, 88, 150 }, -- 小径（暗灰）
    map_shortcut  = { 158, 62, 52, 180 },  -- 捷径（锈红）
    map_node      = { 198, 156, 82, 255 }, -- 节点默认（旧铜金）
    map_current   = { 148, 188, 118, 255 },-- 当前位置（暗绿亮）
    map_dest      = { 218, 178, 92, 255 }, -- 订单目标（亮金）
    map_unknown   = { 58, 55, 50, 120 },   -- 未知节点
    map_fog       = { 32, 30, 28, 200 },   -- 迷雾

    -- 路线可视化
    map_route_fastest  = { 82, 168, 218, 200 },  -- 蓝色
    map_route_safest   = { 108, 188, 96, 200 },  -- 绿色
    map_route_balanced = { 198, 168, 82, 200 },  -- 金色
    map_route_manual   = { 178, 128, 218, 200 }, -- 紫色
    map_waypoint       = { 178, 128, 218, 255 }, -- 途经点
    map_route_glow     = { 255, 255, 255, 40 },  -- 发光底色

    -- 旅行中路线
    map_route_active   = { 82, 198, 168, 220 },  -- 青绿（剩余路线）
    map_route_done     = { 88, 82, 72, 120 },     -- 暗灰（已走过）
    map_truck          = { 255, 210, 80, 255 },   -- 亮金（卡车位置）
}

M.sizes = {
    font_title  = 22,
    font_large  = 18,
    font_normal = 14,
    font_small  = 12,
    font_tiny   = 10,

    padding       = 16,
    padding_small = 8,
    padding_large = 24,

    radius       = 8,
    radius_small = 4,
    radius_large = 12,

    border = 1,
}

return M
